def transcribe_stub(audio_path: str) -> str:
    # Placeholder standing in for ai/transcribe.py's transcribe().
    # Same signature (audio_path: str -> str) so swapping is one line later.
    return f"[STUB TRANSCRIPT] This is placeholder text for {audio_path}."
