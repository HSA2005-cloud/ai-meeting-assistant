# Direct dependencies only; pip resolves the rest. Kept intentionally lean so
# the service fits a 512 MB (Render free-tier) instance — transcription and
# embeddings run via the Gemini API, so there is NO local ML stack
# (torch / sentence-transformers / faster-whisper) loaded into memory.

# Web server
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4

# Config / auth
python-dotenv==1.2.2
PyJWT==2.13.0
cryptography==49.0.0          # required for ES256 JWT verification (auth.py)

# Data / services
supabase==2.31.0
google-genai==2.12.1          # transcription, embeddings, summaries
chromadb==1.5.9               # local vector store for meeting chat
imageio-ffmpeg==0.6.0         # bundled ffmpeg for audio extraction
